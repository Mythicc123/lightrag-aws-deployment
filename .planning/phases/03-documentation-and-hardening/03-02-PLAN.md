---
phase: 03-documentation-and-hardening
plan: "02"
type: execute
wave: "2"
depends_on:
  - "03-01"
files_modified:
  - .gitignore
autonomous: true
requirements:
  - DOCS-06
must_haves:
  truths:
    - ".gitignore excludes .env files"
    - ".gitignore excludes rag_storage/ directory"
    - ".gitignore excludes terraform state files and .terraform/ directories"
    - ".gitignore excludes __pycache__/ directory"
  artifacts:
    - path: ".gitignore"
      contains: [".env", "rag_storage/", ".terraform/", "*.tfstate", "__pycache__/"]
---

<objective>
Verify and, if necessary, update the existing .gitignore to ensure all required exclusions are present per DOCS-06. The existing .gitignore already covers most items -- this plan audits and ensures completeness.

Addresses requirement: DOCS-06 (.gitignore excludes .env, rag_storage/, terraform/.terraform/, *.tfstate, __pycache__/)
</objective>

<context>
@.gitignore

Existing .gitignore already contains:
- .env, .env.local, .env.*.local
- rag_storage/, data/rag_storage/, data/inputs/
- infrastructure/.terraform/ (the actual path, not terraform/.terraform/)
- infrastructure/.terraform.lock.hcl
- *.tfstate, *.tfstate.backup, *.tfstate.d/
- __pycache__/, *.pyc, *.pyo, .pytest_cache/, .coverage
- *.log, /var/, .DS_Store, Thumbs.db, node_modules/
- tests/__pycache__/, tests/.pytest_cache/

Note: DOCS-06 specifies "terraform/.terraform/" but the actual project uses "infrastructure/" as the Terraform directory. The existing .gitignore uses "infrastructure/.terraform/" which is correct for this project's layout.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update .gitignore for DOCS-06 completeness</name>
  <files>.gitignore</files>
  <read_first>
    @.gitignore
  </read_first>
  <action>
Read the existing .gitignore. Then verify it contains the following required entries (per DOCS-06):

1. ".env" -- covers .env files with API keys and secrets
2. "rag_storage/" -- covers the LightRAG data directory synced to/from S3
3. "*.tfstate" -- covers Terraform state files
4. "infrastructure/.terraform/" -- covers the Terraform working directory (DOCS-06 says "terraform/.terraform/" but the actual path is "infrastructure/.terraform/"; the existing entry is correct)
5. "__pycache__/" -- covers Python bytecode cache directories

Required entries and their current status (from existing file read above):
- ".env" -- PRESENT (line 2: ".env")
- "rag_storage/" -- PRESENT (line 7: "rag_storage/")
- "*.tfstate" -- PRESENT (line 21: "*.tfstate")
- "infrastructure/.terraform/" -- PRESENT (line 19: "infrastructure/.terraform/") -- correct path
- "__pycache__/" -- PRESENT (line 12: "__pycache__/")

Since all required entries are present, confirm the existing file is correct and verify with the automated check.

If any entries are missing, add them using the Edit tool, preserving all existing content.
</action>
  <verify>
    <automated>
      grep -q "\.env" .gitignore && \
      grep -q "rag_storage/" .gitignore && \
      grep -q "\.tfstate" .gitignore && \
      grep -q "\.terraform/" .gitignore && \
      grep -q "__pycache__/" .gitignore && \
      echo ".gitignore DOCS-06 compliance PASSED"
    </automated>
  </verify>
  <done>
    .gitignore correctly excludes all DOCS-06 required items: .env, rag_storage/, *.tfstate, .terraform/ (infrastructure/.terraform/), __pycache__/
  </done>
</task>

</tasks>

<verification>
Run: `grep -E "\.env|rag_storage/|\.tfstate|\.terraform/|__pycache__/" .gitignore` returns all required patterns.
</verification>

<success_criteria>
- [ ] DOCS-06: .gitignore excludes .env (API keys/secrets)
- [ ] DOCS-06: .gitignore excludes rag_storage/ (LightRAG data directory)
- [ ] DOCS-06: .gitignore excludes *.tfstate files (Terraform state)
- [ ] DOCS-06: .gitignore excludes .terraform/ directory (Terraform working directory)
- [ ] DOCS-06: .gitignore excludes __pycache__/ (Python bytecode cache)
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-and-hardening/03-02-SUMMARY.md`
</output>
